reserved:
  8000:  gmp-api            # base, NO TOCAR
  9000:  factory-api
  11434: aria-ollama        # compartido, NO TOCAR
  5432:  gmp-postgres       # base, NO TOCAR
  6379:  gmp-redis          # base, NO TOCAR

ranges:
  custom_api:
    start: 8101
    end: 8199
  custom_postgres:
    start: 5433
    end: 5499
  custom_redis:
    start: 6380
    end: 6449

allocations: {}              # la fábrica escribe aquí cada asignación (project_id, port, fecha)

rule: "validar con 'ss -tlnp' antes de asignar y antes de cada deploy"
