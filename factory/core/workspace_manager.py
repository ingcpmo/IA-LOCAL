"""
Gestión del ciclo de vida de workspaces (factory/workspaces/<project_id>/).

TODO F4:
- create_workspace(project_id, base_tag) → template_export + template_engine + claude_bridge
- get_workspace_status(project_id) → estado del workspace
- close_workspace(project_id) → backup tar.gz en backups/factory/ + borrar
"""
